import React from "react";
import "./Order.scss";

import Layout from "./layout/Layout";

export default function Order() {
  return (
    <Layout>
      <div className="order-page page-container">
        <h2>Order Page that I didn't have enough time to build.</h2>
      </div>
    </Layout>
  );
}
